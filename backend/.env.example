# Public — safe to ship to the browser, RLS is what actually protects data.
SUPABASE_URL=https://zzztbpbvxfpyzuxxlkdh.supabase.co
SUPABASE_ANON_KEY=

# Secret — server-side/admin scripts only (e.g. seeding admin_users, bulk
# imports that need to bypass RLS). NEVER expose this in frontend code.
SUPABASE_SERVICE_ROLE_KEY=
