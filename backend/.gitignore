node_modules/
dist/
.env

# Generated by `npm run parse -- --all --out reports/`. Regenerate any time;
# these are for reading, not for review — committing them would put a large
# derived diff in front of every real change.
reports/
